# task-from-work
No2

def square_numbers(numbers):
    return list(map(lambda x: x**2, numbers))
numbers = [1, 2, 3, 4, 5]
squared_numbers = square_numbers(numbers)
print(squared_numbers)  # [1, 4, 9, 16, 25]

Вот объяснение того, как это работает: 
  функция map принимает два аргумента: функцию и итерируемый список. В данном случае функция - это лямбда-функция, которая принимает один аргумент x и возвращает его квадрат (x**2).
  Итерабель - это входной список чисел. Функция map применяет лямбда-функцию к каждому элементу входного списка, эффективно возводя каждое число в квадрат.
  Функция list используется для преобразования объекта map,возвращаемого функцией map, в список. Вот пример использования:

